function check(a,b) {
    if (a > b) {
        alert('So thu nhat lon hon so thu hai')
    } else if (a <= b) {
        var result = (a + b);
        alert('Tong hai so la' + result);
    } else {
        alert('nhap lai so');
    }
}